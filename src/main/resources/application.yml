spring:
  datasource:
    url: jdbc:mysql://localhost:3331/capstone?serverTimezone=Asia/Seoul&characterEncoding=UTF-8
    username: roomit  # 👉 여기 중요! Docker MySQL 설정과 일치해야 함
    password: 1234
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true

server:
  port: 8081
