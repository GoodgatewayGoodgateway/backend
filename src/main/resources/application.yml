spring:
  datasource:
    url: jdbc:mysql://localhost:3331/capstone?serverTimezone=Asia/Seoul&characterEncoding=UTF-8
    username: roomit
    password: 1234
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true

server:
  port: ${PORT:8080}

