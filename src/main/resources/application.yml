spring:
  datasource:
    url: jdbc:mysql://localhost:3331/capstone
    username: root
    password: 1234
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect

#server:
#  port: 8081
server:
  port: ${PORT:8080}
